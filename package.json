{
  "name": "sidequest-supply-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": "node scripts/generate-header-manifest.js",
    "build": "vite build && node scripts/copy-static-assets.js",
    "dev": "vite",
    "preview": "vite preview"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  },
  "devDependencies": {
    "vite": "^5.4.21"
  }
}
